# The images live in the cluster's Quay (private repositories): the mirrored push/pull secret is linked to
# the namespace's default ServiceAccount so every pod can pull them.
apiVersion: v1
kind: ServiceAccount
metadata: { name: default, namespace: haveniq }
imagePullSecrets:
  - { name: quay-push-rhdh }
